<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jinyou.mapper.admin.operation.StationMapper">
    <select id="list" resultType="com.jinyou.pojo.admin.operation.Station">
        select * from station
        <where>
            <if test="query.stationName != null and query.stationName != ''">
                and station_name like concat('%', #{query.stationName}, '%')
            </if>
            <if test="query.status != null">
                and status = #{query.status}
            </if>
            and create_user = #{userId}
        </where>
        ORDER BY sort DESC
    </select>
    <select id="listAll" resultType="com.jinyou.pojo.admin.operation.Station">
        select * from station
        where create_user = #{userId}
        and status = 1
        order by sort desc
    </select>
    <select id="listByStationIds" resultType="com.jinyou.pojo.admin.operation.Station">
        select * from station
        where station_id in
        <foreach collection="stationIds" item="sid" open="(" separator="," close=")">
            #{sid}
        </foreach>

        <!-- 这里改成数组的 .length 就不报错了！-->
        <if test="stationIds != null and stationIds.length > 0">
            ORDER BY FIELD(station_id,
            <foreach collection="stationIds" item="sid" separator=",">
                #{sid}
            </foreach>
            )
        </if>
    </select>
</mapper>